import { useEffect, useRef } from 'react';
import * as THREE from 'three';

export function CosmicBackground() {
  // Particle background removed per global request — let CSS aurora handle visuals.
  return null;
}